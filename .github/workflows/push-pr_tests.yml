name: "Push/PR: Tests"

on:
  push:
    branches: [main]
    paths:
      - "NNekoWeaponIcons/**/*.cs"
      - "NNekoWeaponIcons/**/*.csproj"
      - "NNekoWeaponIcons/**/*.resx"
  pull_request:
    branches: [main]
    paths:
      - "NNekoWeaponIcons/**/*.cs"
      - "NNekoWeaponIcons/**/*.csproj"
      - "NNekoWeaponIcons/**/*.resx"
    types: [ready_for_review, opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  Build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        build_mode: ["Release", "Debug"]
        dalamud-branch: ["release", "stg"]
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Install Dalamud dev build
        run: |
          mkdir -p ~/.xlcore/dalamud/Hooks/dev
          curl -L https://goatcorp.github.io/dalamud-distrib/dev/latest.zip -o dalamud.zip
          unzip dalamud.zip -d ~/.xlcore/dalamud/Hooks/dev

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Package
        run: dotnet publish --configuration Release
